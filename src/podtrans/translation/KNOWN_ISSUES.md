# Translation Module - Known Issues

## ✅ Issue #1: API 返回翻译数量不匹配 (已解决)

**状态**: ✅ 已解决 (v0.2.0)
**解决方案**: 单段落重试机制
**解决时间**: 2025-11-27

### 问题描述 (历史记录)
使用 DashScope (qwen3-max) 翻译时，API 经常返回的翻译数量少于输入的片段数量。

### 复现条件
- 模型：qwen3-max, qwen-coder-plus
- 批次大小：50-100 个片段
- 现象：API 返回 97-98 个翻译，但发送了 100 个片段

### 测试记录

#### 测试 1: 全文翻译（248 片段 → 1 批次）
```
输入：248 segments
API 返回：
  - 第1次重试：238 translations (丢失 10 个)
  - 第2次重试：239 translations (丢失 9 个)
  - 第3次重试：238 translations (丢失 10 个)
结果：失败
```

#### 测试 2: 智能分批（248 片段 → 3 批次，每批 100/100/48）
```
批次 1：
  输入：100 segments
  API 返回：
    - 第1次重试：98 translations (丢失 2 个)
    - 第2次重试：97 translations (丢失 3 个)
    - 第3次重试：97 translations (丢失 3 个)
  结果：失败
```

### 可能原因

1. **API 输出长度限制**
   - DashScope API 可能有最大输出 token 限制
   - 大量翻译结果接近限制时会被截断

2. **JSON 格式生成不稳定**
   - qwen 系列模型在生成大型 JSON 数组时不够稳定
   - 可能在中途停止生成

3. **Response Format 限制**
   - 使用 `response_format={"type": "json_object"}` 可能有隐藏限制

### ✅ 最终解决方案 (v0.2.0)

**实施时间**: 2025-11-27
**核心机制**: 单段落重试 + 双模式支持

#### 实施内容

1. **单段落重试机制** (`_retry_missing_segments()`)
   - 检测到翻译数量不匹配时自动触发
   - 逐个重试丢失的段落
   - 失败时使用降级策略 (`[UNTRANSLATED]` 标记)

2. **双模式支持**
   - JSON 模式: `response_format={"type": "json_object"}`
   - 文本模式: 使用 `|||` 分隔符或换行解析
   - 环境变量: `TRANSLATION_USE_JSON_MODE=true/false`

3. **详细日志记录**
   - 记录丢失段落的索引
   - 记录丢失段落的文本内容
   - 追踪重试过程和结果

4. **配置优化**
   - 批次大小: 100 → 50 (提升稳定性)
   - 模型配置: `qwen-coder-plus` → `qwen-max`

#### 实际效果

**测试结果** (2025-11-27):
```
输入: 248 segments
输出: 248 segments  ✅
丢失率: 0.00% (从 0.81% 改善)

重试统计:
- 批次 4: 丢失 1 个 → 重试成功 ✓
- 批次 5: 丢失 1 个 → 重试成功 ✓
```

**对比数据**:
| 版本 | 丢失率 | 数据完整性 |
|------|--------|-----------|
| v0.1.0 | 0.81% (2/248) | ⚠️ 不完整 |
| v0.2.0 | 0.00% (0/248) | ✅ 100% 完整 |

---

### 历史解决方案 (已废弃)

<details>
<summary>v0.1.0 - 部分接受策略 (已废弃)</summary>

**版本 v0.1.0 - 部分接受策略**
```python
# translator.py line 202-208 (旧版本)
if len(translations) != len(segments):
    logger.warning(f"{missing_count} segments will be skipped.")
    # 只使用已返回的翻译，丢弃缺失的片段
```

**行为**：
- 接受 API 返回的部分结果
- 丢失的片段不会出现在最终输出中
- 记录警告日志但继续执行

**影响**:
- ✅ **优点**：翻译流程不会因少量丢失而完全失败
- ⚠️ **缺点**：最终翻译结果会缺少部分片段（约 2-3% 丢失率）
- 📊 **数据完整性**：对于 248 片段的播客，预计丢失 6-9 个片段

**废弃原因**: 数据完整性问题,已被单段落重试机制取代

</details>

---

## 🔄 后续优化计划

虽然核心问题已解决,但仍有优化空间:

### 短期优化 (1-2 周)

#### 1. Prompt 工程优化
- 添加 few-shot examples 提升格式遵守
- 测试不同 prompt 模板的效果
- 外部化 prompt 配置支持自定义

#### 2. 翻译缓存系统
- 避免重复翻译相同内容
- 降低 API 成本 30-50%
- 提升重处理速度

#### 3. 模型对比测试
- 测试 `qwen-plus`, `qwen-turbo` 的效果
- 记录丢失率、质量、速度、成本
- 找到最优模型配置

### 中期优化 (1 个月)

#### 4. 翻译质量评估
- 自动计算质量指标
- 异常检测 (长度比异常、未翻译标记等)
- 质量报告生成

#### 5. 性能优化
- 并行批次处理 (异步 API 调用)
- 预期速度提升 2-3 倍

### 长期探索

#### 6. 抽象翻译接口
- 支持多种翻译 API (OpenAI, Claude, 本地模型)
- 降级策略和成本优化

#### 7. 流式翻译
- 支持超长播客边翻译边输出
- 减少内存占用

---

## 📚 相关文档

- **改进总结**: `/TRANSLATION_IMPROVEMENTS.md` - v0.2.0 完整改进记录
- **测试脚本**: `/scripts/test_translation_modes.py` - 模式对比测试
- **集成测试**: `/tests/integration/test_translator.py` - 9 个测试用例
- **配置说明**: `/.env.example` - 环境变量配置

---

**文档版本**: v0.2.0
**最后更新**: 2025-11-27
**状态**: ✅ 核心问题已解决,持续优化中
